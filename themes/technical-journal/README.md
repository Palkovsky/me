# Technical Journal

Alternative Hugo theme implementing the warm, bookish technical-journal proposal.

Preview it without changing the site's configured default theme:

```powershell
hugo server --theme technical-journal --port 1315
```

The theme keeps the reading-progress rail and enhanced code blocks from Engineering Magazine, restyled for the journal's paper, rust, ink, and muted-blue palette.
