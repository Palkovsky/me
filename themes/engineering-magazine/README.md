# Engineering Magazine

Alternative Hugo theme implementing the diagram-led Engineering Magazine proposal.

Preview it without changing the site's default theme:

```powershell
hugo server --theme engineering-magazine --port 1314
```

The theme includes dedicated homepage, archive, article, taxonomy, term, and 404 layouts. The current `minimal-blog` theme remains configured in the project root while the alternatives are being reviewed.
